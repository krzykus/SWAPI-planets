const assert = require('assert');

const planetHelper = require('../src/helpers/planet.helper');

const planetData = {}

const comparePlanetData = (picked,stub) => {
    
    if(picked.hasOwnProperty('')){

    }
}

describe('Test data pickers', () => {
 it('pickPlanetData should return correct data', () => {
        assert.ok('TODO');
    });
    it('TODO');
});